# ============================================
# REQUIRED ENVIRONMENT VARIABLES
# ============================================

# Database Configuration
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/pharmatrack?retryWrites=true&w=majority

# Server Configuration
PORT=5000
NODE_ENV=development

# Security & JWT
JWT_SECRET=your_super_secure_random_string_here_min_32_chars
JWT_EXPIRES=7d
SALT_ROUNDS=10

# CORS Configuration (comma-separated origins)
CORS_ORIGINS=http://localhost:5000,https://yourdomain.com

# ============================================
# OPTIONAL ENVIRONMENT VARIABLES
# ============================================

# Logging
LOG_LEVEL=info

# Email Configuration (for future use)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# ============================================
# DEPLOYMENT NOTES
# ============================================
# For production on Render.com:
# 1. Set NODE_ENV=production
# 2. Set JWT_SECRET to a strong random string (min 32 chars)
# 3. Configure MONGO_URI with your MongoDB Atlas connection
# 4. Set CORS_ORIGINS to your domain
# 5. All other variables have defaults
#
# For development:
# 1. Set NODE_ENV=development
# 2. Use local MongoDB or MongoDB Atlas
# 3. CORS_ORIGINS can be * for development (not recommended for production)
# ============================================
